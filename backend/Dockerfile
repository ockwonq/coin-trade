FROM node:18-alpine

WORKDIR /app

# 의존성 파일 복사
COPY package*.json ./

# 의존성 설치
RUN npm ci --only=production

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 5000

# 환경 변수
ENV NODE_ENV=production

# 애플리케이션 실행
CMD ["node", "src/server.js"]
